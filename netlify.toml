# Static site (no build) + serverless function for the CMS GitHub login
[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Always serve the latest editor config + content (never cache these)
[[headers]]
  for = "/admin/config.yml"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/admin/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/content/content.json"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# The pages + loader must never be stale, or edits appear "not to apply"
[[headers]]
  for = "/booking.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/content.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
